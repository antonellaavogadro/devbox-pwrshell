name: install-by-uri
command: "./install-by-uri.ps1 -Uri {{uri}}"
inputs:
  uri:
    type: string
    defaultValue: ""    
    required: true
  url:
    Type: string
    defaultValue: ""    
    required: true
